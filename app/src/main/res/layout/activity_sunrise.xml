<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sunrise_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true">

    <View
        android:id="@+id/sunrise_sky"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0B0C1E" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/sunrise_moon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:text="🌙"
            android:textSize="72sp" />

        <org.fossify.commons.views.MyTextView
            android:id="@+id/sunrise_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/sunrise_fade_in"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/bigger_text_size" />
    </LinearLayout>

    <org.fossify.commons.views.MyTextView
        android:id="@+id/sunrise_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="48dp"
        android:alpha="0.7"
        android:text="@string/sunrise_hint"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/normal_text_size" />

</FrameLayout>
