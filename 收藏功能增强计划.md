# 收藏功能增强计划（睡眠站台 Android · v1.3.7 候选）

> 2026-09-12 整理。背景：网站端「电子安眠药」上线本机收藏后，站主提出 APP 端更需要收藏。
> 核查结论：**APP 端收藏已完整实现，不需要从零开发**。本计划只补三处差距（两处与网站端拉齐，一处补自身）。

## 一、现状盘点（已实现，勿重复开发）

| 层 | 位置 | 已有能力 |
|---|---|---|
| 数据层 | `app/src/main/kotlin/org/fossify/clock/helpers/RelaxStore.kt` | SharedPreferences `relax_favorites` 存 JSON；`RelaxItem` 增/删/改/导入合并（id+URL 双去重）；`CommunityPick` 独立存储 + 1–5 评分 |
| 界面层 | `app/src/main/kotlin/org/fossify/clock/fragments/RelaxFragment.kt` | 睡前页三入口 hub；FAVORITES 分区 = 手动添加（LinkParser 粘贴解析 + 标题抓取）、长按编辑/删除；PICKS 分区长按 = 评分弹窗 |
| 导入导出 | `app/src/main/kotlin/org/fossify/clock/helpers/RelaxDataIO.kt` | 按分区导出/导入 `sleep_station_favorites.json`（v1.3.3） |
| 双语 | `res/values/strings.xml` + `res/values-zh-rCN/strings.xml` | 我的收藏 / 添加收藏 / 空态文案齐全 |

## 二、增强项

### 1. 精选/推荐条目一键收藏（P0，约 0.5 天）

**问题**：想把精选里某条存进「我的收藏」，只能进「添加收藏」对话框手动重填一遍——与网站端"点亮书签"的收藏语义不一致。

**方案**：
- `RelaxFragment.kt` 精选条目长按（现约 437 行，直接弹 `showRateDialog`）改为选项弹窗：`★ 评分` / `收藏到我的收藏`；
- 收藏动作 = `RelaxStore.addCustomItem(pick.title, pick.url)`；
- **顺手修一个现存 bug**：`addCustomItem` 目前不查重，同一 URL 可无限重复添加 → 入口先按 urlKey 查重（见增强项 2），命中则提示「已在收藏中」。

**涉及文件**：`RelaxFragment.kt`、`RelaxStore.kt`、`strings.xml`、`values-zh-rCN/strings.xml`（新增 2 条字符串）。

**验收**：精选条目长按 → 收藏 → FAVORITES 分区出现该条；重复收藏有提示；导入导出对新增条目兼容。

### 2. 统一跨端去重键 urlKey（P0，约 0.5 天）

**问题**：网站端收藏/去重键 = `host 去 www. + pathname 去尾斜杠 + query`（实现见网站 `index.html` 的 `urlKey()`，`tests/dom-test.mjs` 有对齐用例）；APP 端 `mergeCustomItems` / `mergeCommunityPicks` 用 **URL 原串**比较。同一条链接在两端判为两条，将来任何跨端同步都会撞车。

**方案**：
- `RelaxStore.kt` 新增 `fun urlKey(url: String): String`，规则与网站 JS **逐字对齐**：`normalizeUrl` → host 去前缀 `www.` → path 去尾部 `/` → 保留 query；
- `mergeCustomItems` / `mergeCommunityPicks` 的 `knownUrls` 改为按 `urlKey` 集合判重；**存量数据无需迁移**（对旧条目 URL 现算 urlKey 即可）；
- 增强项 1 的查重同样走 urlKey；
- 新增单元测试与网站用例对齐：尾斜杠等价、`www.` 等价、host 大小写等价、query 参与判重。

**涉及文件**：`RelaxStore.kt`、`app/src/test/.../RelaxStoreTest.kt`（新建）。

**验收**：单测全绿；导入含 `www.`/尾斜杠变体的同链接被正确判重。

### 3. 手动收藏补类型标签（P1，约 1 天，可延后）

**问题**：`RelaxItem.type` 字段存在但 `addCustomItem` 不填它——失眠类型星图若铺到 APP，手动收藏是瞎区。

**方案**：`addCustomItem` 增加 `type: String? = null`；添加对话框加可选标签输入（≤12 字；内置 4 键 `anxiety/excitement/physical/noise` + 自定义，与数据契约 v1.1 §2 的 type 域对齐）；导出自动带 type（Gson 序列化已含该字段）；FAVORITES 行沿用 `[typePrefix]` 前缀渲染。

**涉及文件**：`RelaxFragment.kt`、`RelaxStore.kt`、双语 strings。

**验收**：带标签收藏 → 导出 → 另一台导入，标签保留且行首显示前缀。

## 三、明确不做（边界）

- **账号/云同步**：等网站端 Supabase 后端（P1）落地后另立项；`RelaxStore` 头注已预留 "swap point for server sync"。
- **两端收藏互通**：网站 `csc_favorites` 与 APP `relax_favorites` 目前都是本机数据，互通属同步时代功能；增强项 2 是它的硬前置。
- **评分打通**：网站契约 ratings 与 APP 评分同步 = 数据契约 v1.2 议题，本计划不碰。

## 四、排期建议

- **v1.3.7 = 增强项 1 + 2**（合计约 1 天，纯本地逻辑，单元测试基建现成）；
- 增强项 3 视类型星图在 APP 端的排期决定，可顺延到 v1.4.x。
